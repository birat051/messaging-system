/** Central route paths for navigation and redirects (e.g. after **401**). */
export const ROUTES = {
  home: '/',
  settings: '/settings',
  login: '/login',
  register: '/register',
  verifyEmail: '/verify-email',
} as const;
